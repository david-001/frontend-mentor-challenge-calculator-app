import "./Calculator";
import Screen from "./Screen";
import NumpadContainer from "./NumpadContainer";

const Calculator = () => {
  // fsl s;l fsl fslf slfslf slf sfls flsf slfsf sf sfs
  // dasdads
  // podxp popfd
  return (
    <div>
      <Screen className="screen-numpad" />
      <NumpadContainer />
    </div>
  );
};

export default Calculator;
